import { StyleSheet } from 'react-native';

export const main = StyleSheet.create({
  container: {
    width: '100%',
    height: '100%',
    position: 'absolute',
    top: 0,
    right: 0,
    bottom: 0,
    left: 0,
    flex: 1,
    overflow: 'hidden',
    borderColor:'red',
    borderWidth: 0,
  },
});
